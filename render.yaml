services:
  - type: web
    name: battsimlite-backend
    env: python
    region: oregon
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port 8000"
    pythonVersion: 3.10.13
    autoDeploy: true
    branch: main
